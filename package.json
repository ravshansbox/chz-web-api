{
  "name": "chz-web-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "prisma:db:pull": "npx prisma db pull",
    "prisma:db:push": "npx prisma db push",
    "prisma:generate": "npx prisma generate",
    "prisma:reset": "prisma migrate reset --force",
    "start": "nodemon src",
    "test": "echo test"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "@ravshansbox/mini-app": "^2.1.1",
    "path-to-regexp": "^6.2.1",
    "zod": "^3.20.6"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.4",
    "prisma": "^4.10.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
